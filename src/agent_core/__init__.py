"""
agent_core  —  Shared SDK for LLM agent observability, LLM clients, RAG, and tools.

Install:
    pip install agent-core-sdk            # core only (tracing + LLM client)
    pip install agent-core-sdk[rag]       # + ChromaDB + embeddings
    pip install agent-core-sdk[server]    # + FastAPI / Uvicorn
    pip install agent-core-sdk[all]       # everything
"""

__version__ = "0.2.0"
